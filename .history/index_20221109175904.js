//
function introduction(name) {
    const phrase = `This is text ${age} and more text.`;
    return phrase;
    // to do
}

//
function introductionWithLanguage(name, language) {
    // to do
}

//
function introductionWithLanguageOptional(name, language) {
    // to do
}